You'll need to download gwtbox2d, export it as a jar, and include it in your build path before this will work.
Also, don't remove /java/org/jbox2d/emul/java/lang/* from this project or it won't build.
You'll need GWT 2.3.